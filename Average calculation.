grades = [81, 70, 75, 96, 99, 100, 87, 80, 100]
Score_summary = sum(grades)
print(Score_summary)
length_grades = len(grades)
print(length_grades)
average_score = Score_summary / length_grades
print(int(average_score))
a = str("If there is trut, if there is no falsa ! ")
print(average_score in grades,str(a))
